"use client"
import { useState } from "react";
import PlusIcon from "../assets/icons/plus.svg";
import MinusIcon from "../assets/icons/minus.svg";
import clsx from "clsx";
import {motion , AnimatePresence} from 'framer-motion';
const items = [
  {
    question: "Why Choose our Market Place?",
    answer:
      "Our marketplace streamlines the cargo transportation process, reduces costs, and enhances trust between partners. With cutting-edge technology, we ensure seamless operations and an unparalleled logistics experience.",
  },
  {
    question: "How IoTex-DEPIN is used in Cargo Tracking?",
    answer:
      "IoTeX's Decentralized Physical Infrastructure Network (DePIN) utilizes devices like the Pebble Tracker to provide real-time, tamper-proof tracking of cargo. These devices monitor location, temperature, humidity, motion, and light, securely transmitting data to the IoTeX blockchain.",
  },
  {
    question: "Who can use the platform?",
    answer:
      "Shippers, carriers, and warehouse managers.",
  },
  {
    question: "How does JET Token work?",
    answer:
      "It powers payments, rewards, and staking within the ecosystem.",
  },
];

const AccordinationItem = ({question, answer}:{question:string, answer: string}) => {
  const[isOpen, setIsOpen] = useState(false);
  return(
   
    <div className=" py-7 border-b border-white/30" onClick={() => setIsOpen(!isOpen)}>
    <div className="flex items-center ">
      <span className="flex-1 text-lg font-bold">{question}</span>
      {isOpen ? <MinusIcon /> :<PlusIcon />}
      
      </div>
      <AnimatePresence>
      {isOpen && (
        <motion.div 
        initial={{opacity: 0, height: 0, marginTop: 0}}
        animate={{opacity: 1, height: "auto" , marginTop:'16px'}}
        exit={{opacity: 0, height: 0, marginTop: 0}}
          >{answer}</motion.div>

      )}
      </AnimatePresence>
    
  </div>
  
    
  )
}

export const FAQs = () => {
  return (
    <div className="bg-black text-white py-[72px] sm:py-24 bg-gradient-to-b from-[#5D2CA8] to-black " id="faqs" >
      <div className="container">
        <h2 className="text-5xl sm:text-6xl sm:w-[648px] mx-auto text-center text-white tracking-tighter">
          Frequently Asked Questions
        </h2>
        <div className="mt-12 max-w-[648px] mx-auto">
         {items.map(({question, answer}) => (
            <AccordinationItem question={question} answer={answer} key={question}/>
         ))}
        </div>
      </div>
    </div>
  )
};
